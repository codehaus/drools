These are the drools configuration files that need to be in the classes\META-INF folder when using IKVM to compile the java classes so the .NET semenatics library can be used.

Hopefully in the future, we can configure drools on the fly and use .NET configuration.